# Providers and Authentication

Supported route classes: subscription CLI, API and hybrid. Subscription CLI is preferred to reuse the user's own provider plan. API fallback is never silent. Route configuration stores only executable templates and environment-variable names, not secret values.
