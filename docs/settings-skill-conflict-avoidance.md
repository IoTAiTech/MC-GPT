# Conflict-avoidance contract

This feature branch must not edit PR #19 paths (README.md, CHANGELOG.md,
benchmarks, `tests/common.py`, and the listed PR #19 test/workflow files).

Documentation for this feature lives in new files under `docs/` and
`governance/`.
